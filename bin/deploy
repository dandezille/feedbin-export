#!/usr/bin/env bash

set -euo pipefail

source .env

gcloud functions deploy feedbin-to-todoist --region=europe-west2 --trigger-http --runtime=python37 --entry-point=function --set-env-vars FEEDBIN_USER=$FEEDBIN_USER,FEEDBIN_PASSWORD=$FEEDBIN_PASSWORD,TODOIST_API_KEY=$TODOIST_API_KEY
